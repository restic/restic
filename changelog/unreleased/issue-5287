Enhancement: Automatically rebuild index before `recover`

Before running  the `recover` command, it was previously necessary to manually
run `repair index`. This now happens automatically, such that only `recover` is
necessary.

https://github.com/restic/restic/issues/52897
https://github.com/restic/restic/pull/5296
